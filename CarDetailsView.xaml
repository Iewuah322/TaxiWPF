<Window x:Class="TaxiWPF.Views.CarDetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TaxiWPF.ViewModels"
        Title="Детали автомобиля" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen" MinWidth="1000" MinHeight="600"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        FontFamily="Segoe UI" MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Border Background="#F0EEE9" CornerRadius="10" BorderBrush="#8D837C" BorderThickness="1">
        <Grid>
            <!-- Кнопка закрытия -->
            <Button Content="✕" Command="{Binding CloseCommand}" Style="{StaticResource CloseButton}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,5,0" Panel.ZIndex="100"/>

            <!-- Основной контент -->
            <Grid Margin="40,50,40,50">
                <Border Background="#FFFFFF" 
                        Opacity="0.98"
                        CornerRadius="25" 
                        Padding="0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        BorderBrush="#E0E0E0"
                        BorderThickness="2">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="30" ShadowDepth="8" Opacity="0.25" Color="Black"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Заголовок -->
                        <Border Grid.Row="0" Background="#F8F8F8" CornerRadius="25,25,0,0" Padding="30,20">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🚗" FontSize="28" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="TitleText" 
                                               Text="ДЕТАЛИ АВТОМОБИЛЯ" 
                                               FontSize="24" 
                                               FontWeight="Bold" 
                                               Foreground="#333333"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="Заполните информацию об автомобиле" 
                                           FontSize="14" 
                                           Foreground="#888888" 
                                           Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Основной контент -->
                        <Grid Grid.Row="1" Margin="30,25">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="400"/>
                            </Grid.ColumnDefinitions>

                            <!-- Левая часть - Фото -->
                            <Grid Grid.Column="0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Главное фото -->
                                <Border Grid.Row="0" 
                                        Background="#F5F5F5" 
                                        CornerRadius="15" 
                                        BorderBrush="#E8E8E8" 
                                        BorderThickness="2"
                                        Margin="0,0,0,15">
                                    <Grid>
                                        <Image Source="{Binding Car.MainImageUrl, Converter={StaticResource PathToImageConverter}, FallbackValue='https://i.stack.imgur.com/y9DpT.jpg'}" 
                                               Stretch="Uniform"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"/>
                                        <Border Background="#80000000" 
                                                CornerRadius="15,15,0,0" 
                                                VerticalAlignment="Top"
                                                Padding="15,10"
                                                Visibility="{Binding Car.MainImageUrl, Converter={StaticResource StringToVisibilityConverter}}">
                                            <TextBlock Text="📷 Главное фото" 
                                                       FontSize="13" 
                                                       FontWeight="SemiBold" 
                                                       Foreground="White"/>
                                        </Border>
                                    </Grid>
                                </Border>

                                <!-- Галерея фото -->
                                <Border Grid.Row="1" Background="#FAFAFA" CornerRadius="12" Padding="15">
                                    <StackPanel>
                                        <TextBlock Text="📸 Галерея фото" 
                                                   FontSize="14" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="#333333" 
                                                   Margin="0,0,0,10"/>
                                        <ListBox ItemsSource="{Binding PhotoGallery}" 
                                                 SelectedItem="{Binding SelectedPhotoUrl}"
                                                 Background="Transparent"
                                                 BorderThickness="0"
                                                 ScrollViewer.HorizontalScrollBarVisibility="Auto" 
                                                 ScrollViewer.VerticalScrollBarVisibility="Disabled">
                                            <ListBox.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ListBox.ItemsPanel>
                                            <ListBox.ItemContainerStyle>
                                                <Style TargetType="ListBoxItem">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="ListBoxItem">
                                                                <Border x:Name="border" 
                                                                        Background="White" 
                                                                        CornerRadius="8" 
                                                                        Width="100" 
                                                                        Height="100" 
                                                                        Margin="0,0,10,0"
                                                                        BorderBrush="#E8E8E8"
                                                                        BorderThickness="1"
                                                                        Cursor="Hand">
                                                                    <Image Source="{Binding Converter={StaticResource PathToImageConverter}}" 
                                                                           Stretch="UniformToFill"/>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsSelected" Value="True">
                                                                        <Setter TargetName="border" Property="BorderBrush" Value="#FFC107"/>
                                                                        <Setter TargetName="border" Property="BorderThickness" Value="3"/>
                                                                    </Trigger>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="border" Property="BorderBrush" Value="#FFD700"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ListBox.ItemContainerStyle>
                                        </ListBox>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- Правая часть - Форма -->
                            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
                                <StackPanel>
                                    <!-- Модель авто -->
                                    <TextBlock Text="Модель авто" Style="{StaticResource InputLabel}" Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding Car.ModelName, UpdateSourceTrigger=PropertyChanged}" 
                                             Style="{StaticResource InputField}" 
                                             Margin="0,0,0,20"/>

                                    <!-- Гос. номер -->
                                    <TextBlock Text="Гос. номер" Style="{StaticResource InputLabel}" Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding Car.LicensePlate, UpdateSourceTrigger=PropertyChanged}" 
                                             Style="{StaticResource InputField}" 
                                             Margin="0,0,0,20"/>

                                    <!-- Цвет -->
                                    <TextBlock Text="Цвет" Style="{StaticResource InputLabel}" Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding Car.Color, UpdateSourceTrigger=PropertyChanged}" 
                                             Style="{StaticResource InputField}" 
                                             Margin="0,0,0,20"/>

                                    <!-- Тариф -->
                                    <TextBlock Text="Тариф" Style="{StaticResource InputLabel}" Margin="0,0,0,5"/>
                                    <ComboBox SelectedItem="{Binding Car.Tariff, UpdateSourceTrigger=PropertyChanged}" 
                                              Style="{StaticResource CustomComboBox}"
                                              Margin="0,0,0,20">
                                        <ComboBoxItem Content="Эконом"/>
                                        <ComboBoxItem Content="Комфорт"/>
                                        <ComboBoxItem Content="Бизнес"/>
                                    </ComboBox>

                                    <!-- Двигатель -->
                                    <TextBlock Text="Двигатель" Style="{StaticResource InputLabel}" Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding Car.EngineInfo, UpdateSourceTrigger=PropertyChanged}" 
                                             Style="{StaticResource InputField}" 
                                             Margin="0,0,0,20"/>

                                    <!-- Страховка -->
                                    <TextBlock Text="Страховка" Style="{StaticResource InputLabel}" Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding Car.InsuranceInfo, UpdateSourceTrigger=PropertyChanged}" 
                                             Style="{StaticResource InputField}" 
                                             Margin="0,0,0,25"/>

                                    <!-- Кнопки работы с фото -->
                                    <Border Background="#FFF8E1" CornerRadius="12" Padding="15" Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="📷 Управление фото" 
                                                       FontSize="14" 
                                                       FontWeight="SemiBold" 
                                                       Foreground="#333333" 
                                                       Margin="0,0,0,12"/>
                                            
                                            <Button Content="📤 Выбрать главное фото" 
                                                    Command="{Binding SelectMainPhotoCommand}" 
                                                    Style="{StaticResource PrimaryButton}" 
                                                    Padding="15,10"
                                                    FontSize="13"
                                                    Margin="0,0,0,10"/>
                                            
                                            <Button Content="➕ Добавить фото в галерею" 
                                                    Command="{Binding AddPhotoToGalleryCommand}" 
                                                    Style="{StaticResource PrimaryButton}" 
                                                    Padding="15,10"
                                                    FontSize="13"
                                                    Margin="0,0,0,10"/>
                                            
                                            <Button Content="🗑 Удалить выбранное фото" 
                                                    Command="{Binding RemovePhotoUrlCommand}" 
                                                    Padding="15,10"
                                                    FontSize="13"
                                                    Background="#FFE5E5"
                                                    Foreground="#DC3545"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    Visibility="{Binding SelectedPhotoUrl, Converter={StaticResource StringToVisibilityConverter}}">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="Button">
                                                                    <Border Background="{TemplateBinding Background}" 
                                                                            CornerRadius="10"
                                                                            Padding="{TemplateBinding Padding}">
                                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Background" Value="#DC3545"/>
                                                                            <Setter Property="Foreground" Value="White"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>

                        <!-- Нижняя панель с кнопками -->
                        <Border Grid.Row="2" Background="#F8F8F8" CornerRadius="0,0,25,25" Padding="30,20">
                            <Grid>
                                <Button Content="🗑 УДАЛИТЬ" 
                                        Command="{Binding DeleteCarCommand}" 
                                        Style="{StaticResource DeclineButton}" 
                                        Padding="25,12"
                                        FontSize="14"
                                        HorizontalAlignment="Left"
                                        Visibility="{Binding IsAddMode, Converter={StaticResource InverseBooleanConverter}}"/>

                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="ОТМЕНА" 
                                            Command="{Binding CloseCommand}" 
                                            Style="{StaticResource DeclineButton}" 
                                            Padding="25,12"
                                            FontSize="14"
                                            Margin="0,0,15,0"
                                            Visibility="{Binding IsAddMode, Converter={StaticResource BoolToVis}}"/>

                                    <Button Content="💾 СОХРАНИТЬ" 
                                            Command="{Binding SaveCommand}" 
                                            Style="{StaticResource AcceptButton}" 
                                            Padding="30,12"
                                            FontSize="14"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
