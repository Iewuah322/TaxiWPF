<Window x:Class="TaxiWPF.Views.RegistrationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TaxiWPF.ViewModels"
        xmlns:converters="clr-namespace:TaxiWPF.Converters"
        Title="Регистрация" Height="Auto" Width="420" SizeToContent="Height" WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        FontFamily="Segoe UI" MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBooleanConverter x:Key="InverseBoolConverter"/>
    </Window.Resources>

    <Window.DataContext>
        <vm:RegistrationViewModel/>
    </Window.DataContext>

    <Border Background="#FDF4CD" CornerRadius="10" BorderBrush="#E0CA8A" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="TAXIWPF" Foreground="#A8986B" FontWeight="Bold" FontSize="12" Margin="15,10" VerticalAlignment="Center"/>
            <Button Content="✕" Click="CloseButton_Click" Style="{StaticResource CloseButton}" HorizontalAlignment="Right" VerticalAlignment="Top"/>

            <Border Grid.Row="1" Background="#FFFFFF" Margin="2,0,2,2" CornerRadius="0,0,10,10">
                <StackPanel Margin="40,20">
                    <TextBlock Text="Создание аккаунта" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30" Foreground="#333333"/>

                    <TextBlock Text="Имя пользователя" Style="{StaticResource InputLabel}"/>
                    <Grid>
                        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputField}"/>
                        <TextBlock Text="👤" IsHitTestVisible="False" Style="{StaticResource InputFieldIcon}"/>
                    </Grid>

                    <TextBlock Text="Email" Style="{StaticResource InputLabel}"/>
                    <Grid>
                        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputField}"/>
                        <TextBlock Text="✉️" IsHitTestVisible="False" Style="{StaticResource InputFieldIcon}"/>
                    </Grid>

                    <TextBlock Text="Пароль" Style="{StaticResource InputLabel}"/>
                    <Grid>
                        <PasswordBox Name="PasswordBox" Style="{StaticResource PasswordField}"/>
                        <TextBlock Text="🔑" IsHitTestVisible="False" Style="{StaticResource InputFieldIcon}"/>
                    </Grid>

                    <StackPanel Visibility="{Binding IsDriverRegistration, Converter={StaticResource BoolToVis}}">

                        <TextBlock Text="Фото прав" Style="{StaticResource InputLabel}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding LicensePhotoPath}" Style="{StaticResource InputField}" IsReadOnly="True" Padding="8"/>
                            <Button Grid.Column="1" Content="..." Command="{Binding SelectLicensePhotoCommand}" Style="{StaticResource PrimaryButton}" Width="45" Height="45" Margin="5,0,0,0" VerticalAlignment="Bottom"/>
                        </Grid>

                        <TextBlock Text="Ваше фото" Style="{StaticResource InputLabel}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding ProfilePhotoPath}" Style="{StaticResource InputField}" IsReadOnly="True" Padding="8"/>
                            <Button Grid.Column="1" Content="..." Command="{Binding SelectProfilePhotoCommand}" Style="{StaticResource PrimaryButton}" Width="45" Height="45" Margin="5,0,0,0" VerticalAlignment="Bottom"/>
                        </Grid>

                    </StackPanel>
                    <Button Content="ЗАРЕГИСТРИРОВАТЬСЯ" Command="{Binding RegisterCommand}" Style="{StaticResource PrimaryButton}" Margin="0,30,0,0"/>

                    <Button Command="{Binding ToggleRegistrationModeCommand}" Style="{StaticResource CustomHyperlinkButton}" Margin="0,15,0,0" HorizontalAlignment="Center">
                        <TextBlock>
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="Хотите стать водителем?"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsDriverRegistration}" Value="True">
                                            <Setter Property="Text" Value="Зарегистрироваться как клиент"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>