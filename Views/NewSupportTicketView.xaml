<Window x:Class="TaxiWPF.Views.NewSupportTicketView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Обращение в поддержку" Height="250" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Опишите кратко вашу проблему" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <TextBox Grid.Row="1" x:Name="SubjectTextBox" TextWrapping="Wrap" AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto" MinHeight="80"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Отмена" IsCancel="True" Padding="15,5"/>
            <Button Content="Создать обращение" IsDefault="True" Click="CreateButton_Click" Margin="10,0,0,0" Padding="15,5" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>